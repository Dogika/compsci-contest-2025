/*
after enemies are spawned a listener is created and added to g_listeners
checks if there are no more enemies
if so, spawn next wave of enemies or leave combat

spawn wave of enemies:

a list of spawn enemy functions are created and stored in a list
this is a wave

current time is added to the timestamp of each event when created
for sequential spawning


*/
